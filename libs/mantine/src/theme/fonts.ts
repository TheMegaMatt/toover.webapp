import { MantineThemeOverride } from '@mantine/core';

export const APP_FONTS: MantineThemeOverride = {
	fontFamily: 'Varela Round, Greycliff CF, sans-serif',
	headings: {
		fontFamily: 'Varela Round, Greycliff CF, sans-serif',
	},
};
